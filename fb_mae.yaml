name: fb_mae
channels:
  - defaults
  - pytorch
  - nvidia
  # - xformers
  - conda-forge
dependencies:
  - python=3.9.18
  - pip
  - pip:
    - -r requirements.txt
# don't forget to then manually:
# pip install -e .
# (assuming you're in repo root) to install the dinov2 package
